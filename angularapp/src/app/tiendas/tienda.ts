export interface tiendaCreacionDTO {
  idTienda: number,
  sucursal: string,
  calle: string,
  cp: string,
  colonia: string,
  ciudad: string,
  activo: boolean 
}

export interface tiendaDTO {
  idTienda: number,
  sucursal: string,
  calle: string,
  cp: string,
  colonia: string,
  ciudad: string,
  activo: boolean 
}

export interface tiendaCreacionDTO2 {
  sucursal: string,
  calle: string,
  cp: string,
  colonia: string,
  ciudad: string,
  activo: boolean
}
